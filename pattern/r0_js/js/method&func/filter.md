[[js]] [[js_arr]] 
#исключение-елмасс
```javascript
// for distinct
// Создаем цикл для удаления повторяющихся элементов из массива
for (const i of names) {
  // Проверяем, содержится ли текущий элемент `i` в массиве `r0`
  // Если элемент уже присутствует, пропускаем его добавление
  // Иначе добавляем элемент в массив `r0`
  r0.includes(i) ? 0 : r0.push(i);
}
// .filter((element,index,array)=>element!==array[index-1]);
.filter((e,i,a)=>e!==a[i-1]);
```
